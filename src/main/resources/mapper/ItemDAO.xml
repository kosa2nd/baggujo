<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.baggujo.dao.ItemDAO">
    <insert id="insertItem" parameterType="com.baggujo.dto.ItemInsertDTO">
        <![CDATA[
            insert into item (title, description, status,
            condition, item_category_id, member_id)
            values(#{title}, #{description}, #{itemStatus},
            #{itemCondition}, #{itemCategoryId}, #{memberId})
        ]]>
<!--        <selectKey resultType="long" order="AFTER">-->
<!--            select id from (select id from item order by id desc) where rownum = 1-->
<!--        </selectKey>-->
    </insert>
</mapper>